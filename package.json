{
  "name": "newsites",
  "version": "2.0.0",
  "description": "最近リリースされた Web サイトを自動収集してカテゴリ別に一覧表示する静的サイト",
  "type": "module",
  "private": true,
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "node scripts/build.mjs",
    "collect": "node scripts/collect.mjs",
    "start": "npm run build && node server.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.24.2",
    "sharp": "^0.33.5"
  }
}
